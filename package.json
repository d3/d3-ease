{
  "name": "d3-ease",
  "version": "0.0.1",
  "description": "Easing functions for smooth animation.",
  "main": "build/ease",
  "jsnext:main": "index",
  "repository": {
    "type": "git",
    "url": "https://github.com/d3/d3-ease.git"
  },
  "scripts": {
    "pretest": "mkdir -p build && d3-bundler --format=cjs -- index.js > build/ease.js",
    "test": "faucet `find test -name '*-test.js'`",
    "prepublish": "npm run test"
  },
  "devDependencies": {
    "d3-bundler": "~0.2.5",
    "faucet": "0.0",
    "tape": "4"
  }
}
